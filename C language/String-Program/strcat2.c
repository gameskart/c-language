#include <stdio.h>
#include <conio.h>
#include <string.h>
int main()
{
    char s[20];
    int len, i, n;
    printf("Enter string:  ");
    scanf("%d", &n);
    for (i = 0; i < n; i++)
    {
        scanf("%s\n", s[i]);
    }
    for(i=0;s[i]!='\0';i++)
    {
        for()
    }
    getch();
    return 0;
}